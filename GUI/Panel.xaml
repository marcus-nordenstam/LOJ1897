<Grid
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    HorizontalAlignment="Stretch"
    VerticalAlignment="Stretch">

    <Grid.Resources>
        <ResourceDictionary>
            <!-- Style for TextBox with dark background -->
            <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                <Setter Property="TextBox.Background">
                    <Setter.Value>
                        <SolidColorBrush Color="#FF2C2416"/>
                    </Setter.Value>
                </Setter>
                <Setter Property="TextBox.Foreground">
                    <Setter.Value>
                        <SolidColorBrush Color="#FFD4C4A0"/>
                    </Setter.Value>
                </Setter>
                <Setter Property="TextBox.BorderThickness" Value="2"/>
                <Setter Property="TextBox.BorderBrush">
                    <Setter.Value>
                        <SolidColorBrush Color="#FF6B5A3D"/>
                    </Setter.Value>
                </Setter>
                <Setter Property="TextBox.Padding" Value="10,8,10,8"/>
                <Setter Property="TextBox.FontSize" Value="20"/>
                <Setter Property="TextBox.FontFamily" Value="Theme/Fonts/#PT Root UI"/>
            </Style>
            
            <!-- Style for TextBlock labels -->
            <Style TargetType="TextBlock">
                <Setter Property="TextBlock.Foreground">
                    <Setter.Value>
                        <SolidColorBrush Color="#FFD4C4A0"/>
                    </Setter.Value>
                </Setter>
                <Setter Property="TextBlock.FontSize" Value="24"/>
                <Setter Property="TextBlock.FontFamily" Value="Theme/Fonts/#PT Root UI"/>
                <Setter Property="TextBlock.FontWeight" Value="Bold"/>
                <Setter Property="TextBlock.HorizontalAlignment" Value="Center"/>
                <Setter Property="TextBlock.VerticalAlignment" Value="Center"/>
                <Setter Property="TextBlock.Effect">
                    <Setter.Value>
                        <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="2" Opacity="0.8"/>
                    </Setter.Value>
                </Setter>
            </Style>
            
            <!-- Style for Button with vintage look -->
            <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                <Setter Property="Button.Background">
                    <Setter.Value>
                        <SolidColorBrush Color="#FF4A3C28"/>
                    </Setter.Value>
                </Setter>
                <Setter Property="Button.Foreground">
                    <Setter.Value>
                        <SolidColorBrush Color="#FFD4C4A0"/>
                    </Setter.Value>
                </Setter>
                <Setter Property="Button.BorderThickness" Value="2"/>
                <Setter Property="Button.BorderBrush">
                    <Setter.Value>
                        <SolidColorBrush Color="#FF8B7355"/>
                    </Setter.Value>
                </Setter>
                <Setter Property="Button.FontSize" Value="28"/>
                <Setter Property="Button.FontFamily" Value="Theme/Fonts/#PT Root UI"/>
                <Setter Property="Button.FontWeight" Value="Bold"/>
                <Setter Property="Button.Padding" Value="40,15,40,15"/>
            </Style>
        </ResourceDictionary>
    </Grid.Resources>

    <!-- Menu Container (hidden during gameplay) -->
    <Grid x:Name="MenuContainer">
        <!-- Background with MainMenu.png -->
        <Grid.Background>
            <ImageBrush ImageSource="MainMenu.png" Stretch="UniformToFill"/>
        </Grid.Background>

        <!-- Main Menu Content - Positioned on the right side -->
        <Grid HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,150,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>  <!-- Title -->
                <RowDefinition Height="60"/>    <!-- Spacer -->
                <RowDefinition Height="Auto"/>  <!-- Seed Label + Input (horizontal) -->
                <RowDefinition Height="60"/>    <!-- Spacer -->
                <RowDefinition Height="Auto"/>  <!-- Play Button -->
                <RowDefinition Height="20"/>    <!-- Small Spacer -->
                <RowDefinition Height="Auto"/>  <!-- Fullscreen Button -->
            </Grid.RowDefinitions>

            <!-- Game Title -->
            <TextBlock Grid.Row="0"
                       Text="LEGENDS OF JUSTICE: 1897"
                       FontSize="48"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"/>

            <!-- Seed Label + Input (Horizontal) -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="History Seed:"
                           FontSize="24"
                           VerticalAlignment="Center"
                           Margin="0,0,15,0"/>
                <TextBox x:Name="SeedTextBox"
                         Width="250"
                         Height="50"
                         VerticalAlignment="Center"
                         TextAlignment="Center"
                         Text="12345"/>
            </StackPanel>

            <!-- Play Game Button -->
            <Button Grid.Row="4"
                    x:Name="PlayGameButton"
                    Content="PLAY GAME"
                    HorizontalAlignment="Center"
                    Cursor="Hand"/>

            <!-- Fullscreen Toggle Button -->
            <Button Grid.Row="6"
                    x:Name="FullscreenButton"
                    Content="FULLSCREEN"
                    HorizontalAlignment="Center"
                    Cursor="Hand"
                    FontSize="20"
                    Padding="30,10,30,10"/>
        </Grid>
    </Grid>

    <!-- Talk Indicator (shown when aiming at NPC, outside MenuContainer) -->
    <StackPanel x:Name="TalkIndicator"
                Orientation="Horizontal"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Margin="40,0,0,0"
                Visibility="Collapsed">
        <!-- Black circle with T -->
        <Grid Width="28" Height="28" Margin="0,0,8,0">
            <Ellipse Fill="#E0000000"/>
            <TextBlock Text="T"
                       FontSize="16"
                       FontWeight="Bold"
                       Foreground="White"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center">
                <TextBlock.Effect>
                    <DropShadowEffect ShadowDepth="0" BlurRadius="0"/>
                </TextBlock.Effect>
            </TextBlock>
        </Grid>
        <!-- Talk text -->
        <TextBlock Text="Talk"
                   FontSize="18"
                   Foreground="White"
                   VerticalAlignment="Center">
            <TextBlock.Effect>
                <DropShadowEffect Color="Black" BlurRadius="3" ShadowDepth="1" Opacity="0.9"/>
            </TextBlock.Effect>
        </TextBlock>
    </StackPanel>

    <!-- Dialogue Panel (shown when talking to NPC) -->
    <Grid x:Name="DialoguePanelRoot"
          HorizontalAlignment="Right"
          VerticalAlignment="Stretch"
          Margin="0,50,50,50"
          Width="600"
          Visibility="Collapsed">

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Main dialogue area with dark background -->
        <Grid Grid.Row="0">
            <!-- Dark semi-transparent background -->
            <Border Background="#CC000000" CornerRadius="4"/>
            
            <!-- Content overlay -->
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Dialogue history (scrollable, content at bottom) -->
                <ScrollViewer x:Name="DialogueScrollViewer"
                              Grid.Row="0"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <Grid MinHeight="{Binding ViewportHeight, RelativeSource={RelativeSource AncestorType=ScrollViewer}}">
                        <StackPanel x:Name="DialogueList" VerticalAlignment="Bottom"/>
                    </Grid>
                </ScrollViewer>
                
                <!-- Input area -->
                <TextBox x:Name="DialogueInput"
                         Grid.Row="1"
                         Background="#FF1A1A1A"
                         Foreground="#FFD4C4A0"
                         BorderThickness="0,1,0,0"
                         BorderBrush="#FF4A4A4A"
                         FontFamily="Theme/Fonts/#PT Root UI"
                         FontSize="16"
                         Padding="14,10,14,10"
                         CaretBrush="#FFD4C4A0"/>
            </Grid>
        </Grid>

        <!-- Hint text area (below the panel) -->
        <TextBlock x:Name="DialogueHintText"
                   Grid.Row="1"
                   FontFamily="Verdana"
                   FontSize="16"
                   Foreground="#FFFFFF00"
                   Margin="0,14,0,0"
                   Visibility="Collapsed"/>

    </Grid>

    <!-- Caseboard Panel (shown when C is pressed during gameplay) -->
    <Grid x:Name="CaseboardPanel"
          HorizontalAlignment="Stretch"
          VerticalAlignment="Stretch"
          Visibility="Collapsed"
          ClipToBounds="True">
        
        <!-- Dark fabric background (fixed, doesn't pan/zoom) -->
        <Border Background="#FF0A1520"/>
        
        <!-- Fabric texture overlay (subtle pattern, fixed) -->
        <Border>
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#FF0A1822" Offset="0"/>
                    <GradientStop Color="#FF0D1E2A" Offset="0.5"/>
                    <GradientStop Color="#FF081418" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
        </Border>
        
        <!-- Pannable/Zoomable Content Container -->
        <Canvas x:Name="CaseboardContent" ClipToBounds="False">
            <!-- Transform for pan and zoom -->
            <Canvas.RenderTransform>
                <TransformGroup>
                    <ScaleTransform x:Name="CaseboardZoomTransform" ScaleX="1" ScaleY="1"/>
                    <TranslateTransform x:Name="CaseboardPanTransform" X="0" Y="0"/>
                </TransformGroup>
            </Canvas.RenderTransform>
            
            <!-- Title (positioned in board space) -->
            <TextBlock Canvas.Left="700" Canvas.Top="200"
                       Text="MURDER CARDS"
                       FontSize="42"
                       FontWeight="Bold"
                       Foreground="#FF4A2020"
                       FontFamily="Theme/Fonts/#PT Root UI">
                <TextBlock.Effect>
                    <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="3" Opacity="0.5"/>
                </TextBlock.Effect>
            </TextBlock>
        
            <!-- Case Cards Container (positioned in board space) -->
            <StackPanel x:Name="CaseCardsContainer"
                        Canvas.Left="200" Canvas.Top="300"
                        Orientation="Horizontal">
            
            <!-- Case Card: VICTIM -->
            <Border x:Name="CaseCard_Victim"
                    Width="180" Height="240"
                    Margin="15"
                    Background="#FF1A1A1A"
                    BorderBrush="#FF333333"
                    BorderThickness="2"
                    CornerRadius="4">
                <Grid>
                    <TextBlock Text="VICTIM"
                               FontSize="18"
                               FontWeight="Bold"
                               Foreground="#FF666666"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               FontFamily="Theme/Fonts/#PT Root UI"/>
                </Grid>
            </Border>
            
            <!-- Case Card: CAUSE OF DEATH -->
            <Border x:Name="CaseCard_CauseOfDeath"
                    Width="180" Height="240"
                    Margin="15"
                    Background="#FF1A1A1A"
                    BorderBrush="#FF333333"
                    BorderThickness="2"
                    CornerRadius="4">
                <Grid>
                    <TextBlock Text="CAUSE OF&#x0a;DEATH"
                               FontSize="18"
                               FontWeight="Bold"
                               Foreground="#FF666666"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               TextAlignment="Center"
                               FontFamily="Theme/Fonts/#PT Root UI"/>
                </Grid>
            </Border>
            
            <!-- Case Card: TIME & PLACE -->
            <Border x:Name="CaseCard_TimePlace"
                    Width="180" Height="240"
                    Margin="15"
                    Background="#FF1A1A1A"
                    BorderBrush="#FF333333"
                    BorderThickness="2"
                    CornerRadius="4">
                <Grid>
                    <TextBlock Text="TIME &amp;&#x0a;PLACE"
                               FontSize="18"
                               FontWeight="Bold"
                               Foreground="#FF666666"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               TextAlignment="Center"
                               FontFamily="Theme/Fonts/#PT Root UI"/>
                </Grid>
            </Border>
            
            <!-- Case Card: MEANS -->
            <Border x:Name="CaseCard_Means"
                    Width="180" Height="240"
                    Margin="15"
                    Background="#FF1A1A1A"
                    BorderBrush="#FF333333"
                    BorderThickness="2"
                    CornerRadius="4">
                <Grid>
                    <TextBlock Text="MEANS"
                               FontSize="18"
                               FontWeight="Bold"
                               Foreground="#FF666666"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               FontFamily="Theme/Fonts/#PT Root UI"/>
                </Grid>
            </Border>
            
            <!-- Case Card: MOTIVE -->
            <Border x:Name="CaseCard_Motive"
                    Width="180" Height="240"
                    Margin="15"
                    Background="#FF1A1A1A"
                    BorderBrush="#FF333333"
                    BorderThickness="2"
                    CornerRadius="4">
                <Grid>
                    <TextBlock Text="MOTIVE"
                               FontSize="18"
                               FontWeight="Bold"
                               Foreground="#FF666666"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               FontFamily="Theme/Fonts/#PT Root UI"/>
                </Grid>
            </Border>
            
            <!-- Case Card: SUSPECT -->
            <Border x:Name="CaseCard_Suspect"
                    Width="180" Height="240"
                    Margin="15"
                    Background="#FF1A1A1A"
                    BorderBrush="#FF333333"
                    BorderThickness="2"
                    CornerRadius="4">
                <Grid>
                    <TextBlock Text="SUSPECT"
                               FontSize="18"
                               FontWeight="Bold"
                               Foreground="#FF666666"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               FontFamily="Theme/Fonts/#PT Root UI"/>
                </Grid>
            </Border>
            
            </StackPanel>
        </Canvas>
        
        <!-- HUD hint at bottom (fixed, doesn't pan/zoom) -->
        <TextBlock Text="[C] Close Caseboard    [N] Add Note    [Scroll] Zoom    [Drag] Pan"
                   FontSize="16"
                   Foreground="#FFFFFF00"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Bottom"
                   Margin="0,0,0,30"
                   FontFamily="Verdana"/>
        
    </Grid>

</Grid>
